/node_modules
/.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# 카카오톡으로 받은 원본 사진 (용량이 커서 저장소에 넣지 않는다)
# 웹용으로 줄인 것은 public/team/ 에 있다
KakaoTalk_*.jpg

# 피그마 원본 zip (내려받은 것)
figma-files.zip

# 로고 원본 스크린샷 (public/brand/ 에 정리본이 있다)
Screenshot-logo.png
